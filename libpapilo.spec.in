Name:           libpapilo
Version:        @PAPILO_VERSION_MAJOR@.@PAPILO_VERSION_MINOR@.@PAPILO_VERSION_PATCH@
Release:        1%{?dist}
Summary:        PaPILO Presolve Library - C API

License:        Apache-2.0
URL:            https://github.com/Jij-Inc/libpapilo
Source0:        %{name}-%{version}.tar.gz

BuildRequires:  cmake >= 3.14
BuildRequires:  gcc-c++
BuildRequires:  make

%description
libpapilo provides PaPILO (Parallel Presolve for Integer and Linear Optimization)
as a presolve-only shared library with a C API for easy integration with other
solvers and optimization tools.

%package devel
Summary:        Development files for %{name}
Requires:       %{name}%{?_isa} = %{version}-%{release}

%description devel
The %{name}-devel package contains header files for developing
applications that use %{name}.

%prep
%autosetup

%build
%cmake \
    -DCMAKE_BUILD_TYPE=Release \
    -DBUILD_SHARED_LIBS=ON
%cmake_build --target libpapilo

%install
%cmake_install

# Remove files we don't want to package
rm -rf %{buildroot}%{_includedir}/papilo
rm -rf %{buildroot}%{_libdir}/cmake/papilo
rm -f %{buildroot}%{_libdir}/libpapilo-core.so*

%files
%license LICENSE
%doc README.md
%{_libdir}/libpapilo.so.*

%files devel
%{_includedir}/libpapilo.h
%{_libdir}/libpapilo.so

%changelog
* @DATE@ Jij Inc. <dev@j-ij.com> - @PAPILO_VERSION_MAJOR@.@PAPILO_VERSION_MINOR@.@PAPILO_VERSION_PATCH@-1
- Initial RPM package for libpapilo C API